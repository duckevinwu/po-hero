import java.awt.*;

// the interface that lists the signature that every square object should have

public interface Square {
    
    // all squares should have a draw function
    public void draw(Graphics g);
}